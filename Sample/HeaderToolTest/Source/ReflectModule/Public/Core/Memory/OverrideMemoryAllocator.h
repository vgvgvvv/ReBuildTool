// Stub of engine's Core/Memory/OverrideMemoryAllocator.h — the RHT-generated
// module-level glue (ResetEngineExtension/<Module>.cpp) #includes it but the
// generated code does not use any memory-manager API, so an empty header is
// enough for it to compile without the full engine runtime.
#pragma once
