// ReflectObject.cpp — compiles the annotated header to verify the generated
// reflection glue (pulled in via ReflectObject.extension.h) links cleanly with
// the stub macros in ReflectMacros.h.
#include "ReflectObject.h"
